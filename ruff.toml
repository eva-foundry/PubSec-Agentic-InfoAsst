target-version = "py312"
line-length = 120

[lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = [
  "E501",  # line-too-long — not worth gating on for a transit repo
  "E402",  # module-import-not-at-top-of-file — some services legitimately import after sys.path setup
]
